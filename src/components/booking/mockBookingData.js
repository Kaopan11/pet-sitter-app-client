/**
 * Mock คงไว้สำหรับ Day 3+ (guest ว่างจนกว่าจะดึง /users/me)
 * Day 2: pets / sitter มาจาก API แล้ว — ไม่ใช้ MOCK_PETS / MOCK_SITTER ใน Flow
 */

/** ค่าว่างเริ่มต้น — ไม่ใส่ชื่อปลอม */
export const MOCK_GUEST = {
  name: "",
  email: "",
  phone: "",
};
